const config = require("config")
const a: string = config.get("openAiApiKey")
console.log("test", a)

const run = () => {
  // .protoファイル(複数指定可)の読み込み

  // testcaseファイル(複数指定可)の読み込み

  // プロンプトの作成

  
}